/*Insert the element from the unsorted array to its correct position in the sorted array.*/
/*

#include<iostream>
using namespace std;

int main()
{
    int n;
    int temp;
    cout<<"Enter the size of the array; ";
    cin>>n;
    int arr[n];
    for(int i=0;i<n;i++)
    {
        cin>>arr[i];
    }
    for(int i=1;i<n;i++)
    {
        for(int j=0;j<i;j++)
        {
            if(arr[i] < arr[j])
            {
                temp=arr[i];
                 arr[i]=arr[j];
                 arr[j]=temp;
              

             }
        }
        
        cout<<"\n";
        for(int i=0;i<n;i++)
        {
            cout<<arr[i]<<" ";
        }
        cout<<"\n";

        
    }
    cout<<"Sorted array:\n ";
    for(int i=0;i<n;i++)
    {
        cout<<arr[i]<<" ";
    }

    return 0;
}
*/
//fastest three horses.
/*You have 25 horses (find which horse run fast)
at max 5 can race together.
Minimum races for fastest 3 
ANS=> */

